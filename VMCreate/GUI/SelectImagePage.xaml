<!-- SelectImagePage.xaml (new file) -->
<Page x:Class="VMCreate.SelectImagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:VMCreate"
      Title="Select Virtual Machine Image">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Select a Virtual Machine Image" Style="{StaticResource TitleText}"/>
        <TextBox Grid.Row="1" x:Name="SearchTextBox" Margin="0,10,0,10" FontFamily="Segoe UI" FontSize="14"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="494"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- Gallery list -->
            <Grid Grid.Column="0">
                <ListBox x:Name="GalleryListBox" SelectionMode="Single" ScrollViewer.VerticalScrollBarVisibility="Auto" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemContainerStyle="{StaticResource GalleryItemStyle}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ListBox}}" Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="450" Margin="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Image Source="{Binding SymbolUri, Converter={StaticResource ImageConverter}}" Width="80" Height="50" Margin="0,0,0,0" VerticalAlignment="Center" />
                                    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource ExplanationText}" FontWeight="Bold" TextAlignment="Left"/>
                                        <TextBlock Text="{Binding Publisher}" Style="{StaticResource PublisherText}" TextAlignment="Left"/>
                                    </StackPanel>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding FileType}" Style="{StaticResource PillLabelStyle}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <!-- Loading overlay shown until all gallery loaders have completed -->
                <Border Background="#CC1E1E2E"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Loading gallery…"
                                   Foreground="White"
                                   FontFamily="Segoe UI"
                                   FontSize="18"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Fetching available images from the internet"
                                   Foreground="#FFAAAAAA"
                                   FontFamily="Segoe UI"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
            <StackPanel Grid.Column="1" Margin="10,0,10,0">
                <Image x:Name="DetailScreenshot" Width="400" Height="199" Margin="0,0,0,10"/>
                <TextBlock x:Name="DetailName" Style="{StaticResource ExplanationText}" FontWeight="Bold" Margin="0,0,0,-5"/>
                <TextBlock x:Name="DetailPublisher" Style="{StaticResource ExplanationText}" Margin="0,0,0,-5"/>
                <TextBlock x:Name="DetailVersion" Style="{StaticResource ExplanationText}" Margin="0,0,0,-5"/>
                <TextBlock x:Name="DetailLastUpdated" Style="{StaticResource ExplanationText}" Margin="0,0,0,-5"/>
                <TextBlock x:Name="DetailDownloadUrl" Style="{StaticResource ExplanationText}" Margin="0,0,0,-5"/>
                <TextBlock x:Name="DetailDescription" Style="{StaticResource ExplanationText}" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="cancelButton" Content="Cancel" Style="{StaticResource CommonButton}" Click="cancelButton_Click"/>
            <Button x:Name="nextButton" Content="Next &gt;" Style="{StaticResource CommonButton}" Margin="5,0,0,0" Click="nextButton_Click"/>
        </StackPanel>
    </Grid>
</Page>